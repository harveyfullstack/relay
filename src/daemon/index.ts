/**
 * @deprecated Import from '@agent-relay/daemon' instead.
 *
 * This file re-exports from the @relay/daemon package for backward compatibility.
 * All daemon functionality has been moved to packages/daemon/.
 */

// Core daemon infrastructure (per-project)
export * from '@agent-relay/daemon';
