/**
 * @relay/user-directory
 *
 * User directory service for per-user credential storage on workspace volumes.
 */

export {
  UserDirectoryService,
  getDefaultDataDir,
  getUserDirectoryService,
  createUserDirectoryService,
} from './user-directory.js';
