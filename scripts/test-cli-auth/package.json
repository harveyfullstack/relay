{
  "name": "cli-oauth-test",
  "version": "1.0.0",
  "description": "CLI OAuth flow testing for agent-relay (uses relay-pty binary)",
  "type": "module",
  "scripts": {
    "test": "tsx ci-test-runner.ts",
    "test:local": "tsx test-oauth-flow.ts"
  },
  "dependencies": {
    "tsx": "^4.19.0"
  }
}
